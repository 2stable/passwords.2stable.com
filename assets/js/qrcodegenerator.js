// Function to load the QR Code library and generate a QR code function loadAndGenerateQRCode() { var script = document.createElement('script'); script.src = 'https://cdnjs.cloudflare.com/ajax/libs/qrcode-generator/1.4.4/qrcode.min.js'; script.onload = function () { var typeNumber = 10; // Type number (1 to 40) var errorCorrectionLevel = 'H'; // Error correction level ('L', 'M', 'Q', 'H') var qr = qrcode(typeNumber, errorCorrectionLevel); qr.addData(window.location.href); qr.make(); document.getElementById('qrcode').innerHTML = qr.createImgTag(); }; document.body.appendChild(script); } // Ensure the script runs after the document is fully loaded window.onload = loadAndGenerateQRCode;